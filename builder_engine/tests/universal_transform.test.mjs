import assert from "node:assert/strict"; import test from "node:test"; import {resizeTransform} from "../frontend/workspace/transform_math.js"; const b={x:50,y:50,width:40,height:20},bounds={width:400,height:800}; test("e cambia ancho",()=>{const r=resizeTransform(b,{x:40,y:200},bounds,"e");assert.equal(r.width,50);assert.equal(r.height,20);}); test("s cambia alto",()=>{const r=resizeTransform(b,{x:200,y:80},bounds,"s");assert.equal(r.width,40);assert.equal(r.height,30);}); test("nw cambia ambos",()=>{const r=resizeTransform(b,{x:-40,y:-80},bounds,"nw");assert.equal(r.width,50);assert.equal(r.height,30);});
